
  # High-Fidelity PWA Design

  This is a code bundle for High-Fidelity PWA Design. The original project is available at https://www.figma.com/design/HDCZEjG84rMLJab9YOvquW/High-Fidelity-PWA-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  